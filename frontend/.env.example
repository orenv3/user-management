# Copy to .env.local (gitignored). Never commit real credentials.

# Redact this email in ResultPanel only (match SEED_ADMIN1_EMAIL in root .env)
VITE_PRIVATE_ADMIN_EMAIL=your-private-admin@example.com

# Optional: pre-fill login form in local dev only
# VITE_DEMO_EMAIL=
# VITE_DEMO_PASSWORD=
