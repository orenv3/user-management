logging:
  level:
    root: INFO

spring:
  datasource:
    url: jdbc:postgresql://db:5432/user_management_db
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

